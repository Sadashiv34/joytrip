{
  "version": 2,
  "builds": [
    {
      "src": "vercel-build.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "public"
      }
    }
  ],
  "routes": [
    { "src": "/_next/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "/landing.html" }
  ]
}
